export default function getBaseFolder() {
	// var vscode = require("vscode");
	// if (vscode.workspace.workspaceFolders !== undefined) {
	// 	const workspaceFolder = vscode.workspace.workspaceFolders[0]?.uri?.fsPath;
	// 	return workspaceFolder;
	// }
	// return "";
}
